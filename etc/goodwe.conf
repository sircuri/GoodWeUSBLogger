[inverter]
loglevel = #{Inverter.LogLevel}
pollinterval = #{Inverter.PollInterval}
vendorId = #{Inverter.VendorId}
modelId = #{Inverter.ModelId}
logfile = #{Inverter.LogFile}

[mqtt]
server = #{MQTT.Server}
port = #{MQTT.Port}
topic = #{MQTT.Topic}
clientid = #{MQTT.ClientId}
